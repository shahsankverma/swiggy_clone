import React from 'react'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'


const FontAwesomeComponent = ({fontIcon}) => {
    
  return <FontAwesomeIcon icon={fontIcon} />
}

export default FontAwesomeComponent;